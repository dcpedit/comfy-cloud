# ComfyUI EC2 Deployment Configuration Example
# Copy this to .env and update with your values

# Your EC2 key pair name (must exist in your AWS account)
EC2_KEY_NAME=your-keypair-name

# Your security group ID (must allow SSH and HTTP access)
EC2_SECURITY_GROUP=sg-xxxxxxxxxxxxxxxxx

# AWS region for deployment
AWS_REGION=us-east-1

# EC2 instance type (choose based on GPU and compute requirements)
# g6.xlarge: 1x NVIDIA L4 (24GB VRAM) - Good for smaller models
# g4dn.2xlarge: 1x NVIDIA T4 (16GB VRAM) - Cost-effective for development
# g5.2xlarge: 1x NVIDIA A10G (24GB VRAM) - Balanced performance
# p3.2xlarge: 1x NVIDIA V100 (16GB VRAM) - High performance
EC2_INSTANCE_TYPE=g6.xlarge

# EBS volume size in GB (default: 150GB for large AI models)
# Minimum recommended: 100GB for basic models
# 150GB+: For large model collections with ComfyUI custom nodes
EC2_VOLUME_SIZE=150

# S3 bucket for model storage (hardcoded in scripts but configurable here)
S3_MODELS_BUCKET=comfyui-models-dp